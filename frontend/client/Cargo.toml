[package]
name = "machine-launcher-client"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }

[dependencies.machine-launcher-common]
path = "../../common"
version = "*"
