[package]
name = "machine-launcher-common"
version = "0.1.0"
edition = "2021"

[features]
utoipa = ["dep:utoipa"]

[dependencies]
serde = { version = "1", features = ["derive"] }
utoipa = { version = "5", optional = true }
